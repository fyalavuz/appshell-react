## What changed

<!-- One or two sentences. What does this do that the code did not do before? -->

## Why

<!-- The problem, not the patch. Link an issue if there is one. -->

## Checks

- [ ] `npx turbo build test lint typecheck` passes
- [ ] Tests cover the change (a fix has a test that failed before it)
- [ ] `pnpm changeset` added for anything that changes the published package
- [ ] Docs and prop tables updated if the public API moved
