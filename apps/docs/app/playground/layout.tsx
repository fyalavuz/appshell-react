export const metadata = {
  title: "Playground",
  description:
    "Mix every AppShell header behavior, theme, speed, and footer variant live — and copy the code.",
};

export default function PlaygroundLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
